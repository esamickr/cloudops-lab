apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cloudops-lab.fullname" . }}-test-connection"
  labels:
    {{- include "cloudops-lab.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cloudops-lab.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
