services:

  app:
    build: .
    container_name: cloudops-api
    expose:
      - "5000"

  nginx:
    image: nginx:latest
    container_name: cloudops-nginx
    ports:
      - "8080:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - app
